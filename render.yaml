services:
  - type: web
    name: game-web-api
    runtime: python
    rootDir: backend
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: PYTHON_VERSION
        value: 3.13.7
      - key: DATABASE_URL
        fromDatabase:
          name: game-web-db
          property: connectionString
      - key: ADMIN_EMAILS
        sync: false
      - key: FIREBASE_PROJECT_ID
        value: web-app-1001b
      - key: FIREBASE_WEB_API_KEY
        value: AIzaSyCi6ie_HNT357VrkWiGk7cxyVPxwAJxBUI
      - key: JWT_SECRET_KEY
        generateValue: true

databases:
  - name: game-web-db
    plan: free
